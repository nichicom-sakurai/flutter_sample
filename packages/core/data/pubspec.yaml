name: core_data
description: This package provides the repository layer.
version: 1.0.0
publish_to: "none"

## 便宜的にFlutterPackageとしていますが、DartPackageとして扱ってください。
environment:
  sdk: 3.3.1
  flutter: 3.19.3

dependencies:
  collection: 1.18.0
  core_common:
    path: ../common
  core_model:
    path: ../model
  core_network:
    path: ../network
  flutter:
    sdk: flutter
  riverpod_annotation: 2.3.5

dev_dependencies:
  build_runner: 2.4.8
  flutter_test:
    sdk: flutter
  riverpod_generator: 2.4.0

flutter:
